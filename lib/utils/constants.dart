import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

// Padding
double kPadding = 16.0.sp;
double kSmallPadding = 5.sp;
EdgeInsets kPaddingEdgeInsets = EdgeInsets.all(kPadding);

// Radius
double kRadius = 14.0.sp;
double kSmallRadius = 7.0.sp;
BorderRadius kBorderRadius = BorderRadius.all(Radius.circular(kRadius));
BorderRadius kSmallBorderRadius = BorderRadius.all(Radius.circular(kSmallRadius));

double iconSize = 20.sp;
